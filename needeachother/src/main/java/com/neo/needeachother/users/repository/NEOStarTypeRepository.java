package com.neo.needeachother.users.repository;

import com.neo.needeachother.users.entity.NEOStarTypeEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NEOStarTypeRepository extends JpaRepository<NEOStarTypeEntity, Long> {
}