spring:
  profiles:
    include:
      - db
      - oauth
      - jwt

management:
  endpoints:
    web:
      exposure:
        include: prometheus, health, info
#  metrics:
#    tags:
#      application: "neo"